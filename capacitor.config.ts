import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lipastudios.finanzasfacil',
  appName: 'FinanzasFacil',
  webDir: 'dist'
};

export default config;
